import 'package:flutter/material.dart';

class LiquorScreen extends StatelessWidget {
  const LiquorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text("Laquor Screen coming"),
    );
  }
}
